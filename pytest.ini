[pytest]
markers =
    live: marks tests that make real HTTP calls (deselect with -m "not live")
    mocked: marks tests using mocked dependencies (safe to run in CI without network)
    smoke: quick sanity checks — run first
